{
  "name": "diffuse-systems-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --port 3000 --host",
    "build": "npm run lint:prose && vitepress build",
    "preview": "vitepress preview --port 3000 --host",
    "lint:prose": "! grep -rn --include=*.md -F '—' . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.vitepress/cache || (echo '\\nEm dash above. The house style has no em dash: use a comma, a colon or a full stop.' && exit 1)"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
